# sas.projects
End-to-end analysis using SAS Base &amp; STAT. Performed ETL, EDA, and hypothesis testing (Chi-Square). Built regression models using Forward selection (PROC GLMSELECT). Conducted rigorous diagnostics (VIF, Homoscedasticity, Cook’s D). Contains code with detailed statistical interpretation of results.
# 📈 SAS Data Analysis Portfolio (EN)

## 👋 About
Welcome to my portfolio! This repository demonstrates my practical skills in **SAS programming** and **statistical analysis**. Here I apply theoretical knowledge to solve data problems, ranging from data cleaning to building predictive models.

## 🛠 What I can do in SAS
* **Data Preparation:** Importing datasets, handling missing values, and filtering data using `DATA Step` and SQL procedures.
* **Exploratory Analysis (EDA):** Understanding data distribution and finding outliers using `PROC UNIVARIATE` and `PROC SGPLOT`.
* **Statistical Modeling:** Building Linear Regression models to predict outcomes (e.g., Real Estate prices, Wages).
* **Model Selection:** Using **Forward Selection** (`PROC GLMSELECT`) to find the most relevant variables for the model.
* **Diagnostics:** Checking model assumptions using VIF (Multicollinearity), White's Test (Homoscedasticity), and Cook's D.

## 📂 Project Structure
* **Code:** `.sas` files containing the scripts for analysis.
* **Results:** `.pdf` reports showing the output graphs and interpretation of the results.



# 📈 Portfolio SAS projektů: Analýza dat a statistika (CZ)

## 👋 O projektu
Vítejte v mém portfoliu! Tento repozitář slouží jako ukázka mých praktických dovedností v programu **SAS**. Aplikuji zde statistické metody na reálná data – od jejich čištění až po tvorbu prediktivních modelů.

## 🛠 Co v SASu ovládám
* **Příprava dat:** Import dat, ošetření chybějících hodnot a filtrování pomocí `DATA Step` a SQL.
* **Průzkumná analýza (EDA):** Analýza rozdělení dat a detekce odlehlých hodnot pomocí `PROC UNIVARIATE` a grafů (`PROC SGPLOT`).
* **Statistické modelování:** Tvorba lineárních regresních modelů pro predikci (např. ceny nemovitostí, mzdy).
* **Výběr modelu:** Využití metody **Forward Selection** (`PROC GLMSELECT`) pro automatický výběr statisticky významných proměnných.
* **Diagnostika:** Ověření předpokladů modelu – kontrola multikolinearity (VIF), homoskedasticity a vlivných bodů (Cook's D).

## 📂 Obsah repozitáře
* **Kód:** Soubory `.sas` obsahující skripty analýzy.
* **Výsledky:** PDF reporty s grafickými výstupy a interpretací výsledků.
